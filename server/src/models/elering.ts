import db from "../utils/db";

class Elering {
    public async syncEleringData(): Promise<any> {
        const result = await db
        if(result) {
            return true
        }
    }
    
    
}

export default Elering 